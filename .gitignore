# Ignore these files which are expected to be symlinks into files in the hosts/ directory
/systemSettings.nix

# secrets
omv-mediaauthor.secrets
modules/sshkeys.nix
hosts/argon/periphery/periphery.config.toml
home-manager/komodo-periphery.nix